@model IEnumerable<Feed_Bridge.Models.Entities.Donation>

@{
    ViewData["Title"] = "التبرعات";
    ViewData["ActivePage"] = "Donate";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h1 class="mb-4">قائمة التبرعات</h1>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>اسم المتبرع</th>
            <th>رقم التلفون</th>
            <th>اسم المنتج</th>
            <th>صوره المنتج</th>
            <th>تاريخ الانتهاء</th>
            <th>الكمية</th>
            <th>الوصف</th>
            <th>الحذف أو التعديل</th>
            <th>تاريخ التبرع</th>
            <th>التصنيف</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var donation in Model)
        {
              <tr>
                 <td>
                  <a asp-controller="User"
                     asp-action="Profile"
                     asp-route-id="@donation.User?.Id"
                     class="text-primary fw-bold">
                     @donation.User?.UserName
                   </a>
                 </td>
               <td>@donation.Phone</td>
                <td>@donation.Name</td>
                <td>@Url.Action("~/uploads/" + donation.ImgURL)</td>
                <td>@donation.ExpirDate.ToString("yyyy/MM/dd")</td>
                <td>@donation.Quantity</td>
                <td>@donation.Description</td>
                <td>
                    <form asp-action="Delete" asp-controller="Donation" asp-route-id="@donation.Id" method="post">
                        <button type="submit" class="btn btn-danger">حذف</button>
                    </form>
                </td>
                <td>@donation.CreatedAt.ToString("yyyy/MM/dd")</td>
                <td>@donation.Category</td>
            </tr>
        }
    </tbody>
</table>
